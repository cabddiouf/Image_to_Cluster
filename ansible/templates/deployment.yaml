apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-custom
  namespace: default        # <--- ajout important
  labels:
    app: nginx-custom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-custom
  template:
    metadata:
      labels:
        app: nginx-custom
    spec:
      containers:
        - name: nginx
          image: nginx-custom:v1
          ports:
            - containerPort: 80
